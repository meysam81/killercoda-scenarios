# Run containers in the default networking mode

## Run the first nginx

```bash
docker run -d --name nginx1 nginx
```{{exec}}

## Run the second nginx

```bash
docker run -d --name nginx2 nginx
```{{exec}}
